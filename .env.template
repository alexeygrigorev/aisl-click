# Copy this file to `.env` and fill in the values.
# `.env` is gitignored and NEVER committed. scripts/deploy.sh sources it.

# S3 bucket that holds the lambda zip. Globally unique; by convention
# aisl-click-artifacts-<aws-account-id>.
ARTIFACT_BUCKET=aisl-click-artifacts-<account-id>
